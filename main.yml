name:terraform deploymnet
on:
 push:
  branch:[main]
  pull_request:
  branch:[main]
  workflow_dispatch:
  env:
  AWS_ACCESS_KEY=
  AWS_SECERET_ACCESS_KEY=
  jobs:
   build:
   run_on:actions/checkout@v3
   -name:Run a one-linux server
   run:echo Hello,World!
   -name:npm install,build and test
   run: |
         npm install
         npm config
         npm test
   
   
